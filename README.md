# Clock
A desktop app displaying the current time for different timezones

## Stack
For this app the logic is handled by python and the ui is build in pyside6

## How to run the app
First just clone the repo then in a terminal install dependencies and run
the app following next commands:
```
uv sync
python main.py
```