[project]
name = "clock"
version = "1.0.0"
description = "A desktop app displaying a clock for different timezones"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyside6>=6.11.1",
    "tzdata>=2026.2",
]
